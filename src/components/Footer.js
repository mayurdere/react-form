import React from 'react';

export class Footer extends React.Component {
    render() {
        return (
            <div>
                <footer className="footer mt-auto py-3 bg-blue">
                    <div className="container-footer">
                    </div>
                </footer>
            </div>

        );
    }
}

export default Footer;